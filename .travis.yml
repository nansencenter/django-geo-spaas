language: python

services:
  - docker

install:
  - docker build . -t geospaas
  - pip install coveralls

script:
  - docker run --rm -it -v `pwd`:/src geospaas coverage run runtests.py

after_success:
  - coveralls
